services:

  _defaults:
    public: true
    autowire: true

  Github\Client:
    class: Github\Client
    calls:
      - [ authenticate, [ '%env(GITHUB_TOKEN)%', 'null', 'access_token' ] ]

  emteknetnz\JsUpdater\Services\ModuleService: ~
