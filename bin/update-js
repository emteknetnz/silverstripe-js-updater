#!/usr/bin/env php
<?php

require_once __DIR__ . '/../../../autoload.php';

use emteknetnz\JsUpdater\Commands\ShowCommand;
use emteknetnz\JsUpdater\Commands\UpdateCommand;
use Symfony\Component\Console\Application;
use Symfony\Component\Dotenv\Dotenv;

// Allow passing in GITHUB_TOKEN as cli arg e.g. GITHUB_TOKEN=<token> vendor/bin/update-js
$baseDir = dirname(dirname(dirname(dirname(__DIR__))));
$dotenv = (new Dotenv)->load("$baseDir/.env");
$githubToken = getenv('GITHUB_TOKEN');
if ($githubToken) {
    $_ENV['GITHUB_TOKEN'] = $githubToken;
}

$app = new Application();
$app->add(new ShowCommand);
$app->add(new UpdateCommand);
$app->run();
